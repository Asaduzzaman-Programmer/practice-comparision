const price = 9000;

if (price >= 5000){

    // 10% discount 

    const discount = price * 10/100;

    // console.log(discountPrice);

    const discountPrice = price - discount;
    console.log(discountPrice);

}

else(
    console.log(price)
)